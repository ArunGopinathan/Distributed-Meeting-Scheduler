<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    tools:context="edu.uta.cse.distributedmeetingscheduler.OrganizerListActivity">

    <ImageView
        android:id="@+id/logo_image_small"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="3dp"
        android:padding="15dp"
        android:src="@mipmap/dms_logo" />
<ProgressBar
    android:layout_width="wrap_content" android:padding="30dp"
    android:layout_below="@+id/logo_image_small"
    android:layout_height="wrap_content"  android:id="@+id/login_progress" android:visibility="gone"/>

    <ListView android:id="@+id/organizerMeeting" android:layout_width="wrap_content"
        android:layout_below="@+id/logo_image_small"
        android:layout_height="wrap_content" />

</RelativeLayout>
